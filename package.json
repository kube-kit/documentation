{
  "name": "kubekit-documentation",
  "version": "1.0.0",
  "description": "Kubekit documentation",
  "main": "index.js",
  "scripts": {
    "install:gitbook": ". ~/.nvm/nvm.sh && nvm use 18 && npm i && . ~/.nvm/nvm.sh && nvm use 10 && gitbook install",
    "start" : ". ~/.nvm/nvm.sh && nvm use 10 && gitbook serve",
    "build": ". ~/.nvm/nvm.sh && nvm use 10 && gitbook build",
    "deploy": ". ~/.nvm/nvm.sh && nvm use 18 && gh-pages -d _book"
  },
  "author": "kubekit",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
