{
  "name": "kubekit-documentation",
  "version": "1.0.0",
  "description": "Kubekit documentation",
  "main": "index.js",
  "scripts": {
    "start" : "npx --node-version=10 gitbook serve",
    "build": "npx --node-version=10 gitbook build",
    "deploy": ". ~/.nvm/nvm.sh && nvm use 18 && gh-pages -d _book"
  },
  "author": "kubekit",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
